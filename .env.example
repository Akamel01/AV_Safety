# Environment configuration for AV_Safety

# Data API keys (if using any external APIs)
# NHTSA_API_KEY=
# Kaggle_USERNAME=
# Kaggle_KEY=

# Database (if using one)
# DATABASE_URL=

# Output paths
DATA_RAW_DIR=data/raw
DATA_PROCESSED_DIR=data/processed
MODELS_DIR=models
OUTPUT_DIR=outputs

# Debug mode (set to 1 for verbose logging)
DEBUG=0
